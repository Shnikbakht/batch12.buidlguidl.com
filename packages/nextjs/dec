import React from "react";
import Image from "next/image";
import type { NextPage } from "next";
import { Address } from "~~/components/scaffold-eth";
import { FaTwitter, FaLinkedin, FaGithub, FaHashnode } from "react-icons/fa";

const BuilderPage: NextPage = () => {
  const address = "0xB450c1139958772EE65F54F4907e6aCDc7466a4D";
  const bio =
    "I am a passionate Web3 developer, exploring the intersection of blockchain and modern web technologies.";
  const avatarUrl = "https://via.placeholder.com/128";
  const socials = [
    { name: "Twitter", url: "https://twitter.com/jeremiahdsamuel", icon: <FaTwitter /> },
    { name: "LinkedIn", url: "https://linkedin.com/in/jeremiahsamuel", icon: <FaLinkedin /> },
    { name: "GitHub", url: "https://github.com/livinalt", icon: <FaGithub /> },

  ];

  return (
    <div className="flex flex-col items-center justify-center min-h-screen p-6 bg-gradient-to-b from-gray-100 to-gray-300 text-gray-900">
      <div className="max-w-md w-full text-center bg-white rounded-lg shadow-lg p-6">
        <Image
          src={avatarUrl}
          alt="Your Avatar"
          width={128}
          height={128}
          className="rounded-full mx-auto mb-4 border border-gray-300 shadow-sm"
        />

        <h1 className="text-3xl font-bold mb-2">Hello, I'm Jeremiah Samuel</h1>
        <p className="text-base text-gray-700 mb-4">{bio}</p>
        
        <div className="mb-4">
          <span className="font-semibold">Address: </span>
          <Address address={address} />
        </div>

        <div className="space-y-2 mt-4">
          {socials.map((social) => (
            <a
              key={social.name}
              href={social.url}
              target="_blank"
              rel="noopener noreferrer"
              className="flex items-center gap-2 text-blue-500 hover:text-blue-700 text-sm font-medium justify-center"
            >
              <span className="text-lg">{social.icon}</span> {social.name}
            </a>
          ))}
        </div>
      </div>
    </div>
  );
};

export default BuilderPage;
